{
  "//": "The credit-limit (budget). Grant $5; the model loops a metered tool past it → UsageCapExceeded. The lead scenario — the most visceral ('the agent spent real money until it was cut off', PRD Open Q3). PLACEHOLDER shape.",
  "scenario": "budget",
  "grant": { "scope": ["paid.call"], "budget": "$5.00", "ttl": "30m" },
  "downstream": {
    "paid-api": {
      "command": "npx",
      "args": ["-y", "@example/metered-tool-server"]
    }
  },
  "expect_refusal": { "tool": "paid_call", "verdict": "usage-cap-exceeded" }
}
